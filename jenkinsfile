pipeline{
    agent any

    stages{
        stage("checking"){
            steps{
                echo"checkout scm"
            }
        }
        stage("Test"){
            steps{
                sh 'sudo npm install'
                sh 'npm test'
            }
        }
        stage("Build"){
            steps{
                sh 'npm run build'
            }
        }
    }
}
